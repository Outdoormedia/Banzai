# Snapshots extension for Refinery CMS.

## How to build this extension as a gem

    cd vendor/extensions/snapshots
    gem build refinerycms-snapshots.gemspec
    gem install refinerycms-snapshots.gem

    # Sign up for a http://rubygems.org/ account and publish the gem
    gem push refinerycms-snapshots.gem