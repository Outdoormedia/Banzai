<!DOCTYPE html>
%html
  %head
    %meta( charset="utf-8" )
    %title OMA Portal Proof of Concept

    = stylesheet_link_tag    "application", "navigation"
    = javascript_include_tag "application"

    = csrf_meta_tag
    = yield(:head)

  %body

    = render 'shared/header'

    .container

      %section#content
  
        = yield
        = image_tag("main_image.jpg", :alt => "Sample App")

      %canvas#chart( height = '300px')
        = render 'shared/pieChart'

      %section#sidebar
        %section#recent_activity
          %header
            %h1.ir#recent_activity 
              = render '/shared/recent_activity'
              

    = render 'shared/footer'