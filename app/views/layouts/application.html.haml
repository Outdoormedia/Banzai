!!!
%html
  %head
    %title OMA Portal
    = stylesheet_link_tag    "application", "homes"
    = javascript_include_tag "application", "homes"
    = csrf_meta_tag
    = yield(:head)

    %body
      .oma_header
        =image_tag("image003.jpg", :alt => "Sample App", :class => "round")
        #oma_login_invite
          = @login_message
      %br  

      .oma_welcome_outer_panel
        = render '/shared/menu_bar'       

        .oma_welcome_inner_panel
      
          .oma_welcome_message
            = @welcome_message
          .oma_status_message
            = @login_status_message

      #grid
        this 
        is
        a 
        grid
        with 
        something
        else
        here

    = yield

    = render '/shared/footer'
  
        