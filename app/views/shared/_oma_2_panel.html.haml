%section.content
  .image_panel
    = link_to( image_tag("main_image.jpg", size: '600x220', :alt=>"Outdoor Media Association"), refinery.root_path, :title=>"OMA Images" )

  - if ["home", "buy", "learn", "plan", "proof"].include?(@page.slug)
    %canvas#chart( height = '300px')
      = render 'shared/pieChart'
