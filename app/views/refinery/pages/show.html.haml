%section#header
  =raw @page.content_for(:header)

%section#body_content
  =raw @page.content_for(:body)

%section#side_body_content
  =raw @page.content_for(:sidebody)

%section#sidebar_content
  =raw @page.content_for(:side_body)